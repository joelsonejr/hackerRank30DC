
function processData(input) {

    let inputToArray = input.split('\n'); 

    let arrayLength = inputToArray[0];

    let inputString = "";
    
    for (let i = 1; i <= arrayLength; i++){
        
            inputString += array[i];

}

let lastItem = arrayLength - 1; 

function printArray(inputString) {
    
    let outputArray = ""
    
    for(lastItem ; lastItem >= 0; lastItem--){
        outputArray += (jon[lastItem] + ' ');
    }

    console.log(outputArray);
}

